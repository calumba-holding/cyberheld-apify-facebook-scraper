# This file tells Git which files shouldn't be added to source control

.DS_Store
.idea
.vscode
.zed
dist
node_modules
.next/
.brand-preview/

# Python (control plane — platform/*)
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
apify_storage
storage
runs
out
coverage
test-results
playwright-report

# Docker worker state
docker/profiles/
docker/artifacts/
docker/urls.txt

# Per-worker watch config (keep worker.example.json)
farm/config/worker-*.json
!farm/config/worker.example.json

# Added by Apify CLI
.venv

# Local environment — never commit
.env

# Generated extraction scripts — local to each machine
scripts.json

# Local scratch files
output_pascal.json
request.json

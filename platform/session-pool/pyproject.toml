[project]
name = "session-pool"
version = "0.1.0"
description = "Evidence-capture Account & Session Pool — health-scored, quarantine-first (#43)"
requires-python = ">=3.11"
dependencies = [
    "SQLAlchemy>=2.0,<2.1",
    "psycopg[binary]>=3.2",
]

[tool.pytest.ini_options]
pythonpath = ["src", "../metadata-db/src"]
testpaths = ["tests"]

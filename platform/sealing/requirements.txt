cryptography>=42
SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.2
boto3>=1.34          # S3/MinIO WORM backend (#35)
pytest>=8.0
# metadata_db is a sibling package; add ../metadata-db/src to PYTHONPATH (see pyproject).

[project]
name = "processing"
version = "0.1.0"
description = "Evidence-capture Processing Workers — ffmpeg/Whisper/OCR (#45)"
requires-python = ">=3.11"
# Runtime tools (ffmpeg, ffprobe, tesseract, yt-dlp) are system binaries, not pip deps.

[tool.pytest.ini_options]
pythonpath = ["src", "../metadata-db/src", "../sealing/src"]
testpaths = ["tests"]

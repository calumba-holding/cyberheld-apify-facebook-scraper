[project]
name = "capability-router"
version = "0.1.0"
description = "Evidence-capture Capability Router — per-pool queues + concurrency (#38)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
